"use client";import {useState} from "react";import Link from "next/link";export default function WishlistButton({productId}:{productId:string}){const [msg,setMsg]=useState("");async function add(){const r=await fetch("/api/wishlist",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({productId})});const d=await r.json();if(r.status===401)return setMsg("Sign in to save products.");setMsg(r.ok?"Saved to wishlist.":d.error||"Could not save.");}return <div className="mt-3"><button onClick={add} className="w-full border border-white/15 px-6 py-4 text-sm font-bold uppercase tracking-[.15em]">Save to wishlist</button>{msg&&<p className="mt-2 text-sm text-white/45">{msg} {msg.startsWith("Sign in")&&<Link className="text-[#d7ff3f]" href="/auth">Login</Link>}</p>}</div>}