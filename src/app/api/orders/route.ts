import {NextResponse} from "next/server";import {db} from "@/lib/db";import {requireUser} from "@/lib/request-user";
export async function GET(request:Request){const user=await requireUser(request);if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});const orders=await db.order.findMany({where:{userId:user.id},include:{items:true},orderBy:{createdAt:"desc"}});return NextResponse.json({orders});}
export async function POST(request:Request){const user=await requireUser(request);if(!user)return NextResponse.json({error:"Unauthorized"},{status:401});const body=await request.json().catch(()=>({}));try{const order=await db.$transaction(async tx=>{const cart=await tx.cart.findUnique({where:{userId:user.id},include:{items:{include:{variant:{include:{product:true}}}}}});if(!cart||!cart.items.length)throw new Error("EMPTY_CART");for(const item of cart.items)if(item.variant.stock<item.quantity)throw new Error("OUT_OF_STOCK");const address=body.addressId?await tx.address.findFirst({where:{id:body.addressId,userId:user.id}}):null;if(!address)throw new Error("ADDRESS_REQUIRED");const total=cart.items.reduce((sum,item)=>sum+item.variant.product.price*item.quantity,0);const created=await tx.order.create({data:{userId:user.id,total,shippingName:address.fullName,shippingLine1:address.line1,shippingLine2:address.line2,shippingCity:address.city,shippingState:address.state,shippingPostalCode:address.postalCode,shippingCountry:address.country,items:{create:cart.items.map(item=>({variantId:item.variantId,productName:item.variant.product.name,sku:item.variant.sku,unitPrice:item.variant.product.price,quantity:item.quantity}))}},include:{items:true}});for(const item of cart.items){const updated=await tx.productVariant.updateMany({where:{id:item.variantId,stock:{gte:item.quantity}},data:{stock:{decrement:item.quantity}}});if(updated.count!==1)throw new Error("OUT_OF_STOCK");}await tx.cartItem.deleteMany({where:{cartId:cart.id}});return created;});return NextResponse.json({order},{status:201});}catch(e){const m=e instanceof Error?e.message:"";const error=m==="EMPTY_CART"?"Cart is empty":m==="OUT_OF_STOCK"?"One or more items are out of stock":m==="ADDRESS_REQUIRED"?"Choose a valid shipping address":"Could not create order";return NextResponse.json({error},{status:409});}}
