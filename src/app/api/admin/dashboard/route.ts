import {NextResponse} from "next/server";import {db} from "@/lib/db";import {requireAdmin} from "@/lib/admin";export async function GET(r:Request){if(!await requireAdmin(r))return NextResponse.json({error:"Forbidden"},{status:403});const [products,customers,orders,revenue,lowStock]=await Promise.all([db.product.count({where:{active:true}}),db.user.count({where:{role:"CUSTOMER"}}),db.order.count(),db.order.aggregate({_sum:{total:true},where:{status:{in:["PAID","PROCESSING","SHIPPED","DELIVERED"]}}}),db.productVariant.findMany({where:{stock:{lte:5}},include:{product:{select:{name:true}}},orderBy:{stock:"asc"},take:10})]);return NextResponse.json({metrics:{products,customers,orders,revenue:revenue._sum.total||0},lowStock});}