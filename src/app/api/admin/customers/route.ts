import {NextResponse} from "next/server";import {db} from "@/lib/db";import {requireAdmin} from "@/lib/admin";
export async function GET(request:Request){if(!await requireAdmin(request))return NextResponse.json({error:"Forbidden"},{status:403});const users=await db.user.findMany({where:{role:"CUSTOMER"},select:{id:true,name:true,email:true,createdAt:true,orders:{select:{total:true,status:true}}},orderBy:{createdAt:"desc"}});const customers=users.map(u=>({id:u.id,name:u.name,email:u.email,createdAt:u.createdAt,orders:u.orders.length,totalSpent:u.orders.filter(o=>["PAID","PROCESSING","SHIPPED","DELIVERED"].includes(o.status)).reduce((s,o)=>s+o.total,0)}));return NextResponse.json({customers});}
