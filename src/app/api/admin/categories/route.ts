import {NextResponse} from "next/server";import {db} from "@/lib/db";import {requireAdmin} from "@/lib/admin";export async function GET(r:Request){if(!await requireAdmin(r))return NextResponse.json({error:"Forbidden"},{status:403});return NextResponse.json({categories:await db.category.findMany({orderBy:{name:"asc"}})});}export async function POST(r:Request){if(!await requireAdmin(r))return NextResponse.json({error:"Forbidden"},{status:403});const {name,slug}=await r.json();if(!name||!slug)return NextResponse.json({error:"name and slug required"},{status:400});return NextResponse.json({category:await db.category.create({data:{name,slug}})},{status:201});}