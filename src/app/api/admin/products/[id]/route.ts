import {NextResponse} from "next/server";import {db} from "@/lib/db";import {requireAdmin} from "@/lib/admin";
export async function PATCH(r:Request,{params}:{params:Promise<{id:string}>}){if(!await requireAdmin(r))return NextResponse.json({error:"Forbidden"},{status:403});const {id}=await params,b=await r.json();return NextResponse.json({product:await db.product.update({where:{id},data:{name:b.name,description:b.description,price:b.price,active:b.active,categoryId:b.categoryId,modelUrl:b.modelUrl}})});}
export async function DELETE(r:Request,{params}:{params:Promise<{id:string}>}){if(!await requireAdmin(r))return NextResponse.json({error:"Forbidden"},{status:403});const {id}=await params;await db.product.update({where:{id},data:{active:false}});return NextResponse.json({ok:true});}
