import {NextResponse} from "next/server";import {OrderStatus} from "@prisma/client";import {db} from "@/lib/db";import {requireAdmin} from "@/lib/admin";export async function PATCH(r:Request,{params}:{params:Promise<{id:string}>}){if(!await requireAdmin(r))return NextResponse.json({error:"Forbidden"},{status:403});const {id}=await params,{status}=await r.json();if(!Object.values(OrderStatus).includes(status))return NextResponse.json({error:"Invalid status"},{status:400});return NextResponse.json({order:await db.order.update({where:{id},data:{status}})});}