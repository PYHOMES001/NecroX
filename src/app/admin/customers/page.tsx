"use client";import {useEffect,useState} from "react";import Link from "next/link";
type C={id:string;name:string|null;email:string;createdAt:string;orders:number;totalSpent:number};
export default function AdminCustomers(){const [items,setItems]=useState<C[]>([]);const [msg,setMsg]=useState("Loading customers...");useEffect(()=>{fetch("/api/admin/customers").then(async r=>{const d=await r.json();if(!r.ok){setMsg(r.status===403?"Admin access required.":d.error||"Could not load customers.");return;}setItems(d.customers||[]);setMsg("");});},[]);const money=(n:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(n/100);return <main className="min-h-screen px-6 pb-24 pt-24 md:px-12"><Link href="/admin" className="text-sm text-white/45">← Dashboard</Link><h1 className="mt-8 text-5xl font-black">CUSTOMERS</h1>{msg&&<p className="mt-5 text-white/50">{msg}</p>}<div className="mt-10 overflow-x-auto border border-white/10"><table className="min-w-full text-left text-sm"><thead className="border-b border-white/10 text-xs uppercase tracking-[.15em] text-white/35"><tr><th className="p-4">Customer</th><th className="p-4">Joined</th><th className="p-4">Orders</th><th className="p-4">Paid value</th></tr></thead><tbody>{items.map(c=><tr key={c.id} className="border-b border-white/10 last:border-0"><td className="p-4"><b>{c.name||"NecroX Customer"}</b><p className="mt-1 text-white/40">{c.email}</p></td><td className="p-4 text-white/55">{new Date(c.createdAt).toLocaleDateString()}</td><td className="p-4">{c.orders}</td><td className="p-4 font-bold">{money(c.totalSpent)}</td></tr>)}</tbody></table></div></main>}