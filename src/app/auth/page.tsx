"use client";import {FormEvent,useState} from "react";import Link from "next/link";export default function AuthPage(){const [mode,setMode]=useState<"login"|"register">("login");const [msg,setMsg]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const body={name:f.get("name"),email:f.get("email"),password:f.get("password")};setMsg("Authenticating...");const r=await fetch("/api/auth/"+mode,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const d=await r.json();if(r.ok){location.href="/account";return;}setMsg(d.error||"Authentication failed.");}return <main className="grid min-h-screen place-items-center px-6 py-24"><div className="w-full max-w-xl border border-white/10 bg-white/[.025] p-8 md:p-12"><Link href="/" className="text-sm text-white/40">← NECROX</Link><p className="mt-12 text-xs uppercase tracking-[.3em] text-[#d7ff3f]">Member access</p><h1 className="mt-3 text-5xl font-black">{mode==="login"?"WELCOME BACK":"JOIN NECROX"}</h1><form onSubmit={submit} className="mt-10 space-y-3">{mode==="register"&&<input name="name" placeholder="Name" className="w-full border border-white/15 bg-transparent p-4 outline-none focus:border-[#d7ff3f]"/>}<input name="email" type="email" required placeholder="Email" className="w-full border border-white/15 bg-transparent p-4 outline-none focus:border-[#d7ff3f]"/><input name="password" type="password" minLength={8} required placeholder="Password, minimum 8 characters" className="w-full border border-white/15 bg-transparent p-4 outline-none focus:border-[#d7ff3f]"/><button className="w-full bg-[#d7ff3f] p-5 font-black uppercase tracking-[.15em] text-black">{mode==="login"?"Login":"Create account"}</button></form>{msg&&<p className="mt-4 text-sm text-white/50">{msg}</p>}<button onClick={()=>{setMode(mode==="login"?"register":"login");setMsg("");}} className="mt-8 text-sm text-white/50">{mode==="login"?"New here? Create an account":"Already a member? Login"}</button></div></main>}