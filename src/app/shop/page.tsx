import Link from "next/link";import {db} from "@/lib/db";import {formatINR} from "@/lib/money";
export const dynamic="force-dynamic";
export default async function Shop(){const products=await db.product.findMany({where:{active:true},include:{variants:true},orderBy:{createdAt:"desc"}});return <main className="min-h-screen px-6 pb-24 pt-32 md:px-12"><div className="mb-16 flex items-end justify-between"><div><p className="text-xs uppercase tracking-[.3em] text-[#d7ff3f]">NecroX / Catalog</p><h1 className="mt-4 text-6xl font-black md:text-8xl">SHOP THE DROP</h1></div><Link href="/" className="text-sm text-white/50">← Home</Link></div><div className="grid gap-5 md:grid-cols-3">{products.map(p=><Link key={p.id} href={"/product/"+p.slug} className="group border border-white/10 bg-[#0c0c0e] p-6"><div className="flex aspect-square items-center justify-center bg-[radial-gradient(circle,#282828,#090909_65%)] text-7xl font-black italic text-white/10 transition duration-500 group-hover:scale-[.98]">NX</div><div className="mt-6 flex justify-between gap-4"><div><p className="text-xs uppercase tracking-[.18em] text-white/35">{p.variants.filter(v=>v.stock>0).length} variants available</p><h2 className="mt-2 text-2xl font-black">{p.name}</h2></div><b>{formatINR(p.price)}</b></div></Link>)}</div></main>}