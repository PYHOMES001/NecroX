"use client";import {FormEvent,useState} from "react";import Link from "next/link";type P={id:string;name:string;slug:string;description:string;price:number;variants:{id:string;color:string;size:string;stock:number}[]};export default function Discover(){const [query,setQuery]=useState("Find me black sneakers under ₹8,000 for everyday wear");const [products,setProducts]=useState<P[]>([]);const [note,setNote]=useState("");const [loading,setLoading]=useState(false);async function ask(e:FormEvent){e.preventDefault();setLoading(true);const r=await fetch("/api/discover",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({query})});const d=await r.json();setProducts(d.products||[]);setNote(r.ok?d.interpretation:d.error);setLoading(false);}return <main className="min-h-screen px-6 pb-24 pt-32 md:px-12"><p className="text-xs uppercase tracking-[.3em] text-[#d7ff3f]">NecroX discovery</p><h1 className="mt-4 max-w-5xl text-6xl font-black md:text-8xl">DON'T FILTER.<br/>JUST ASK.</h1><form onSubmit={ask} className="mt-10 max-w-5xl border border-white/15 bg-white/[.025] p-4 md:flex"><input value={query} onChange={e=>setQuery(e.target.value)} className="min-w-0 flex-1 bg-transparent p-4 text-lg outline-none" placeholder="Describe what you want..."/><button className="bg-[#d7ff3f] px-8 py-4 font-black text-black">{loading?"SEARCHING...":"ASK NECROX"}</button></form>{note&&<p className="mt-4 text-sm text-white/45">Interpretation: {note}</p>}<div className="mt-12 grid gap-4 md:grid-cols-3">{products.map(p=><Link href={"/product/"+p.slug} key={p.id} className="group border border-white/10 p-5"><div className="flex aspect-square items-center justify-center bg-[radial-gradient(circle,#292929,#070707_68%)] text-6xl font-black italic text-white/10 transition group-hover:scale-[.98]">NX</div><h2 className="mt-5 text-2xl font-black">{p.name}</h2><p className="mt-2 line-clamp-2 text-sm leading-6 text-white/45">{p.description}</p><div className="mt-5 flex justify-between"><b>{new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(p.price/100)}</b><span className="text-xs text-[#d7ff3f]">{p.variants.filter(v=>v.stock>0).length} in stock</span></div></Link>)}</div><p className="mt-12 max-w-3xl text-xs leading-5 text-white/30">This hackathon build grounds discovery in the live NecroX catalog. The current interpreter extracts budget, color and descriptive intent before ranking database products; it does not invent products that are not in inventory.</p></main>}